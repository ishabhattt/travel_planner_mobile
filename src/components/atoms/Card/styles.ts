import { StyleSheet } from "react-native";

export const makeStyles = () => {
  return StyleSheet.create({
    mainContainer: {
      backgroundColor: "#fff",
      padding: 16,
      borderRadius: 12,
      marginTop: 10,
    },
  });
};
